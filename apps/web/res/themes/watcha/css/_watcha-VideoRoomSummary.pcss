/*
   watcha! Contenu legacy ciblait `.mx_VideoRoomSummary` (composant supprimé
   en v1.12.21). Le fichier était déjà non-importé dans watcha.pcss (`@import`
   commenté ligne 30), donc inerte. On vide pour la cohérence d'audit.
   !watcha
*/
